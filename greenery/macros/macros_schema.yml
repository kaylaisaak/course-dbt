version: 2

macros:
  - name: sessions_per_event
    description: >
      Unique session count per event
    docs:
      show: true
    arguments:
      - name: column_name
        description: Column to summarize event type
      - name: event_type
        description: Type of event used to summarize by
